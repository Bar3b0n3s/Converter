{
  "table": "GameObjectDisplayInfo",
  "description": "Model and collision box for a world object (doors, chests, buildings). The model column takes an .mdx path for an M2 or a .wmo path for a world object.",
  "id_offset_columns": ["ID"],
  "columns": [
    {"target": ["ModelName", "ModelPath", "FileName"], "type": "string",
     "from": ["FileDataID", "ModelName"], "transform": "model_path"}
  ]
}
